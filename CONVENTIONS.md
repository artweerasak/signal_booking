# CONVENTIONS.md — แนวทางให้ AI พัฒนาระบบจองสนามเทนนิส (signal_booking)

> ไฟล์นี้คือ "คู่มือสั่งงาน AI" — aider จะอ่านไฟล์นี้แล้วพัฒนาโค้ดตามกฎด้านล่างทุกครั้ง
> วิธีใช้:  aider --read CONVENTIONS.md ไฟล์ที่จะแก้    (หรือพิมพ์ /read CONVENTIONS.md ในแชท aider)

## ภาษาและสไตล์
- โค้ดหลักเป็น **PHP** (ฝั่ง server) + HTML/CSS/JS (ฝั่งหน้าเว็บ)
- คอมเมนต์อธิบายเป็น **ภาษาไทย** สั้น กระชับ
- ตั้งชื่อฟังก์ชัน/ตัวแปรเป็นภาษาอังกฤษแบบ snake_case ให้สื่อความหมาย

## ความปลอดภัย (สำคัญที่สุด)
- ต่อฐานข้อมูลด้วย **PDO + prepared statements เสมอ** ห้ามต่อสตริง SQL ตรงๆ (กัน SQL Injection)
- **ตรวจสอบ (validate) ทุกค่าที่รับจากผู้ใช้** — ชื่อ, เบอร์โทร, วันที่, รอบเวลา ก่อนนำไปใช้
- อัปโหลดไฟล์ (สลิป): จำกัดชนิดไฟล์ (jpg/png/pdf), จำกัดขนาด, ตั้งชื่อไฟล์ใหม่ ห้ามใช้ชื่อเดิมจากผู้ใช้
- **ห้าม** แสดง error ดิบ/ข้อมูล DB ให้ผู้ใช้เห็นในโหมดใช้งานจริง

## โครงสร้างระบบ (อ้างอิง)
- `index.php` — หน้าหลัก: เลือกวัน/สนาม/รอบเวลา + ตะกร้า + ฟอร์มจอง
- `process_booking.php` — รับ POST, validate, อัปโหลดสลิป, บันทึก DB (ใช้ Transaction)
- `api/get_slots.php` — คืน JSON สถานะรอบเวลาที่ว่าง/ถูกจอง
- `config/database.php` — การเชื่อมต่อฐานข้อมูล (PDO)
- `check_booking.php`, `checkout.php` — ขั้นตอนตรวจสอบ/ยืนยัน

## กฎการแก้โค้ด
- แก้เท่าที่จำเป็นตามที่สั่ง อย่ารื้อส่วนที่ไม่เกี่ยว
- ถ้าเพิ่มฟีเจอร์ที่แตะฐานข้อมูล ให้ระบุ SQL ที่ต้องเพิ่ม (CREATE/ALTER) ไว้ในคำตอบด้วย
- เขียนโค้ดที่รันได้จริง ไม่ทิ้ง TODO ค้าง เว้นแต่บอกให้ทำ
- ถ้าไม่แน่ใจสเปก ให้ถามก่อนแก้

## สิ่งที่อยากพัฒนาต่อ (backlog — สั่ง aider ทำทีละอย่างได้)
- [ ] ระบบยกเลิกการจอง (cancel booking)
- [ ] แจ้งเตือนอีเมลเมื่อมีการจองใหม่
- [ ] หน้า admin ดูรายการจองทั้งหมด
- [ ] ตรวจสอบสิทธิพิเศษสำหรับกำลังพล/สมาชิก
